name: PHP Composer

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build_and_test:

    runs-on: ubuntu-latest

    services:
      mysql:
          image: mysql:5.7
          env:
              MYSQL_ALLOW_EMPTY_PASSWORD: yes
              MYSQL_DATABASE: easyrogs_test
          ports:
              - 3306
          options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
    - uses: actions/checkout@v2

    - name: Validate composer.json and composer.lock
      run: composer validate

    - name: Cache Composer packages
      id: composer-cache
      uses: actions/cache@v2
      with:
        path: vendor
        key: ${{ runner.os }}-php-${{ hashFiles('**/composer.lock') }}
        restore-keys: |
          ${{ runner.os }}-php-

    - name: Install dependencies
      if: steps.composer-cache.outputs.cache-hit != 'true'
      run: composer install --prefer-dist --no-progress --no-suggest

    - name: Run test suite
      run: composer run-script test
      env:
        APP_ENV: test
        DB_NAME: easyrogs_test
        DB_HOST: 127.0.0.1
        DB_USERNAME: easyrogs
        DB_PASSWORD: ok
        
        ROOTURL: http://easyrogs.local/
        ROOTPATH: /home/rei/projects/easyrogs/
        
        STRIPE_API_KEY: sk_test_y9GxEH84MZXKzw95CLjXO85B00ftdQkI5W
        STRIPE_PUBLISHABLE_KEY: pk_test_WcvnhZHSShn1uFSSrYc0W10F00pQTJh6lP
        STRIPE_WEBHOOK_SIGNING_SECRET: whsec_axgLDqigmEMTklPuSu3u5cPnwBe5UtqW
        PAYMENT_WHITELIST: